# 可恢复批量配音

按脚本批量生成并在每条结束后原子写 Manifest，支持中断恢复和内容指纹缓存。

## 使用方法

1. 连接 Model、ScriptPlan、VoiceBank 和可选生成参数。
2. 24GB 显存通常从 batch size 4–8 开始。
3. 输出 AudioBatch 继续进入时长适配、QA、审核和交付。

## 注意

只有模型、文本、音色、参数和文件都匹配时才命中缓存；不会用旧音频冒充新结果。
