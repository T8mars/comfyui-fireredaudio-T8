# 多 Take 试听评审板

用匿名乱序 A/B/C 播放器保存评分、备注和唯一采用项。

## 使用方法

1. 首次保持采用序号为 0，只运行到评审板并完成盲听。
2. 在节点内点击“采用”，它会写入隐藏的稳定候选 ID。
3. 再次运行后把已评审 AudioBatch 和采用 ID 连接候选采用节点。

## 注意

节点默认不会采用第一条；只有人工点击采用并再次运行后，采用 ID 才会输出。
