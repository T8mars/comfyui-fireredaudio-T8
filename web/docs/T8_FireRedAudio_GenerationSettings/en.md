# Generation settings

Reusable quality preset, seed, audio steps, text tokens, diffusion steps, and CFG.

## How to use

1. Start with `balanced`.
2. Keep the seed and all settings fixed for reproducible delivery.
3. Use the creative candidate pool for exploration instead of turning QA repair into random sampling.

## Important

Higher steps cost more time. Any settings change invalidates matching batch cache entries.
